name: Deploy Frontend
on: 
  push:
    branches: [ staging ]

jobs:
  Deploy-Server:
    runs-on: ubuntu-latest
    steps:
      - run: yarn install
      - run: npm run build
  
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}